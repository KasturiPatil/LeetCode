class Solution:
    def arrayStringsAreEqual(self, word1: List[str], word2: List[str]) -> bool:
        c,d = '',''
        for i in word1:
            c += i

        for i in word2:
            d += i
        if c == d:
            return True
        return False
        
